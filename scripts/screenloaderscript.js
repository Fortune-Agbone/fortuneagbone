//script for webpage preloader

window.addEventListener('load', () => {
    const pageloaderdiv = document.querySelector('.screen-loaderdiv');
    pageloaderdiv.classList.add('screenloader-finish');
});